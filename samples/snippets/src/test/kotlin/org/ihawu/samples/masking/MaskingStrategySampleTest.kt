package org.ihawu.samples.masking

import kotlin.test.Test

class MaskingStrategySampleTest {
    @Test
    fun `hideRemovesField sample compiles and runs`() {
        hideRemovesField()
    }

    @Test
    fun `redactReplacesValue sample compiles and runs`() {
        redactReplacesValue()
    }
}
