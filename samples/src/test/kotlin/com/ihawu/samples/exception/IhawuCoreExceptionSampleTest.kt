package com.ihawu.samples.exception

import kotlin.test.Test

class IhawuCoreExceptionSampleTest {
    @Test
    fun `throwWithMessage sample compiles and runs`() {
        throwWithMessage()
    }

    @Test
    fun `throwWithCause sample compiles and runs`() {
        throwWithCause()
    }
}
